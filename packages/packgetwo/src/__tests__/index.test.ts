import { func } from "../index";
import { funcMult } from "@someinterestingtestnamesorrynpmforthisasdsadasdasdasd/packageone";

describe("test", () => {
  it("should expect", () => {
    expect(func(1, 1)).toEqual(0);
    expect(funcMult(2, 3)).toEqual(6);
  });
});
